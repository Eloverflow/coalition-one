// API routes in TypeScript
import { Router } from 'express';

const router: Router = Router();

// Define API endpoints
// router.get('/posts', postsController.getAllPosts);

export default router;
